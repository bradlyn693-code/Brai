import { useEffect, useMemo, useRef, useState, type ReactNode } from "react";
import { Link, Route, Switch, useLocation } from "wouter";
import { AnimatePresence, motion, useAnimationControls, useMotionValue, useTransform } from "framer-motion";
import {
  ArrowLeft, ArrowRight, Bell, Check, CheckCheck, ChevronRight, CircleDollarSign, CreditCard, Crown,
  Eye, EyeOff, Gift, Heart, HeartHandshake, ImagePlus, Lock, MapPin, Menu, MessageCircle,
  MoreHorizontal, Phone, Search, Send, Settings, ShieldCheck, SlidersHorizontal, Sparkles, Star,
  Trash2, UserRound, UsersRound, Video, WalletCards, X, Zap,
} from "lucide-react";

type Profile = {
  id: number; name: string; age: number; city: string; distance: number; role: string; bio: string;
  interests: string[]; verified: boolean; online: boolean; images: string[]; match?: number;
};
type MatchRecord = Profile & { matchedAt: number; isNew?: boolean };
type ChatMessage = { id: string; sender: "me" | "them"; text?: string; image?: string; gift?: string; at: number; read?: boolean };
type ChatRecord = { userId: number; messages: ChatMessage[]; lastAt: number };

const profileImages = [
  "https://images.unsplash.com/photo-1494790108377-be9c29b29330?auto=format&fit=crop&w=900&q=86",
  "https://images.unsplash.com/photo-1500648767791-00dcc994a43e?auto=format&fit=crop&w=900&q=86",
  "https://images.unsplash.com/photo-1534528741775-53994a69daeb?auto=format&fit=crop&w=900&q=86",
  "https://images.unsplash.com/photo-1506794778202-cad84cf45f1d?auto=format&fit=crop&w=900&q=86",
  "https://images.unsplash.com/photo-1517841905240-472988babdf9?auto=format&fit=crop&w=900&q=86",
  "https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?auto=format&fit=crop&w=900&q=86",
  "https://images.unsplash.com/photo-1531123897727-8f129e1688ce?auto=format&fit=crop&w=900&q=86",
  "https://images.unsplash.com/photo-1504593811423-6dd665756598?auto=format&fit=crop&w=900&q=86",
  "https://images.unsplash.com/photo-1524504388940-b1c1722653e1?auto=format&fit=crop&w=900&q=86",
  "https://images.unsplash.com/photo-1519345182560-3f2917c472ef?auto=format&fit=crop&w=900&q=86",
];

const profiles: Profile[] = [
  { id: 1, name: "Sophia", age: 24, city: "Barcelona", distance: 3, role: "Artist", bio: "Chasing sunlight, making things with my hands, and finding the best cortado in town.", interests: ["Coffee", "Gallery dates", "Surfing"], verified: true, online: true, images: [profileImages[0], profileImages[5], profileImages[8]], match: 96 },
  { id: 2, name: "Mateo", age: 27, city: "Barcelona", distance: 5, role: "Architect", bio: "I collect ceramics, playlists, and restaurant recommendations. Tell me your current favorite.", interests: ["Design", "Foodie", "Vinyl"], verified: true, online: false, images: [profileImages[1], profileImages[7], profileImages[3]], match: 92 },
  { id: 3, name: "Ava", age: 25, city: "Gràcia", distance: 8, role: "Photographer", bio: "Soft mornings, loud laughs, and a camera roll full of tiny adventures.", interests: ["Film", "Travel", "Dogs"], verified: false, online: true, images: [profileImages[2], profileImages[8], profileImages[6]], match: 89 },
  { id: 4, name: "Leo", age: 29, city: "El Born", distance: 10, role: "Chef", bio: "Will cook you pasta and let you pick the playlist. Usually in that order.", interests: ["Cooking", "Jazz", "Plants"], verified: true, online: true, images: [profileImages[3], profileImages[1], profileImages[9]], match: 87 },
  { id: 5, name: "Nora", age: 26, city: "Eixample", distance: 12, role: "Stylist", bio: "Finding color in the everyday and a reason to dress up for breakfast.", interests: ["Vintage", "Fashion", "Brunch"], verified: true, online: false, images: [profileImages[4], profileImages[0], profileImages[6]], match: 91 },
  { id: 6, name: "Maya", age: 30, city: "Poblenou", distance: 14, role: "Curator", bio: "Museum afternoons, big questions, and very serious opinions about gelato.", interests: ["Art", "Museums", "Travel"], verified: false, online: true, images: [profileImages[6], profileImages[2], profileImages[7]], match: 88 },
  { id: 7, name: "Jules", age: 28, city: "Sants", distance: 16, role: "Product designer", bio: "Good at maps, bad at choosing one dessert. Looking for a co-pilot.", interests: ["Design", "Hiking", "Coffee"], verified: true, online: false, images: [profileImages[7], profileImages[9], profileImages[1]], match: 94 },
  { id: 8, name: "Elena", age: 23, city: "Sant Antoni", distance: 18, role: "Writer", bio: "Collecting tiny stories, long walks, and people who notice the moon.", interests: ["Books", "Writing", "Dogs"], verified: true, online: true, images: [profileImages[8], profileImages[4], profileImages[0]], match: 86 },
  { id: 9, name: "Theo", age: 31, city: "Les Corts", distance: 20, role: "Musician", bio: "A soundtrack for every season and a soft spot for a spontaneous road trip.", interests: ["Music", "Road trips", "Vinyl"], verified: false, online: true, images: [profileImages[9], profileImages[3], profileImages[5]], match: 90 },
  { id: 10, name: "Clara", age: 26, city: "Raval", distance: 22, role: "Florist", bio: "Flowers, rainy-day films, and making ordinary Tuesdays feel like a celebration.", interests: ["Plants", "Film", "Cooking"], verified: true, online: false, images: [profileImages[5], profileImages[2], profileImages[4]], match: 93 },
];

const userKey = "couplehearts_user";
const matchKey = "couplehearts_matches";
const chatKey = "couplehearts_chats";
const defaultUser = { email: "demo@couplehearts.com", name: "Alex", coins: 1200 };

function readJson<T>(key: string, fallback: T): T {
  try { const value = localStorage.getItem(key); return value ? JSON.parse(value) as T : fallback; } catch { return fallback; }
}
function getUser() { return readJson(userKey, null as { email: string; name: string; coins?: number } | null); }
function readCoins() { return Math.max(0, Number(getUser()?.coins ?? 1200)); }
function updateCoins(delta: number) {
  const current = getUser() || defaultUser;
  const next = { ...current, coins: Math.max(0, Number(current.coins ?? 0) + delta) };
  localStorage.setItem(userKey, JSON.stringify(next));
  window.dispatchEvent(new CustomEvent("couplehearts:coins", { detail: next.coins }));
  return next.coins;
}
function spendCoins(cost: number) { if (readCoins() < cost) return false; updateCoins(-cost); return true; }
function vibrate(pattern: number | number[] = 18) { if (typeof navigator !== "undefined" && "vibrate" in navigator) navigator.vibrate(pattern); }
function saveMatches(items: MatchRecord[]) { localStorage.setItem(matchKey, JSON.stringify(items)); window.dispatchEvent(new Event("couplehearts:matches")); }
function profileById(id: number) { return profiles.find((profile) => profile.id === id) || profiles[0]; }
function seededMatches(): MatchRecord[] {
  return profiles.slice(0, 4).map((profile, index) => ({ ...profile, matchedAt: Date.now() - index * 3600000, isNew: index < 2 }));
}
function loadMatches() {
  const stored = readJson<MatchRecord[]>(matchKey, []);
  if (stored.length) return stored.map((item) => ({ ...profileById(item.id), ...item }));
  const initial = seededMatches(); saveMatches(initial); return initial;
}
function matchPercent(profile: Profile, mine = ["Coffee", "Travel", "Design", "Film"]) {
  const common = profile.interests.filter((interest) => mine.includes(interest)).length;
  return Math.min(99, 85 + common * 3 + ((profile.id * 7) % 5));
}
function timeLabel(at: number) { const diff = Math.max(0, Date.now() - at); if (diff < 60000) return "now"; if (diff < 3600000) return `${Math.floor(diff / 60000)}m`; if (diff < 86400000) return `${Math.floor(diff / 3600000)}h`; return "Yesterday"; }

function Logo({ compact = false }: { compact?: boolean }) { return <div className={compact ? "brand brand-compact" : "brand"}><span className="brand-mark">💑</span><span>Couple Hearts<span className="brand-heart">♥</span></span></div>; }
function Pill({ children, tone = "rose" }: { children: ReactNode; tone?: "rose" | "green" | "purple" | "gold" }) { return <span className={`pill pill-${tone}`}>{children}</span>; }
function AuthShell({ children, eyebrow, title, subtitle, sideTitle }: { children: ReactNode; eyebrow: string; title: string; subtitle: string; sideTitle: string }) {
  return <div className="auth-shell"><section className="auth-visual"><div className="auth-visual-glow" /><div className="auth-visual-inner"><Logo /><div className="auth-copy"><p className="eyebrow eyebrow-light">{eyebrow}</p><h2>{sideTitle}</h2><p>Slow down, be curious, and find your person in the little things.</p><div className="couple-orbit" aria-hidden="true"><span>♡</span><span>♡</span><div className="orbit-line" /></div><div className="auth-proof"><div className="avatar-stack"><img src={profileImages[0]} /><img src={profileImages[2]} /><img src={profileImages[4]} /></div><span><strong>12k+</strong> couples matched today</span></div></div><p className="auth-footnote">Made for meaningful connections <span>♥</span></p></div></section><section className="auth-panel"><div className="auth-panel-inner"><div className="auth-mobile-logo"><Logo /></div><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p className="auth-subtitle">{subtitle}</p>{children}</div></section></div>;
}
function Login() {
  const [, navigate] = useLocation(); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [showPassword, setShowPassword] = useState(false); const [error, setError] = useState("");
  const submit = (event: React.FormEvent) => { event.preventDefault(); if (!email || password.length < 6) { setError("Please enter an email and a password with 6+ characters."); return; } const existing = getUser(); localStorage.setItem(userKey, JSON.stringify(existing?.email === email ? existing : { email, name: email.split("@")[0] === "demo" ? "Alex" : "Lovely human", coins: 1200 })); navigate("/dashboard"); };
  return <AuthShell eyebrow="Welcome back" title="Welcome back 🥰" subtitle="Your next great conversation might be one sign-in away." sideTitle="Find your couple."><form className="auth-form" onSubmit={submit}><label>Email address<input value={email} onChange={(e) => setEmail(e.target.value)} type="email" placeholder="you@example.com" /></label><label>Password<div className="input-with-action"><input value={password} onChange={(e) => setPassword(e.target.value)} type={showPassword ? "text" : "password"} placeholder="••••••••" /><button type="button" className="input-action" onClick={() => setShowPassword(!showPassword)} aria-label="Toggle password visibility">{showPassword ? <EyeOff size={17} /> : <Eye size={17} />}</button></div></label><div className="form-row"><label className="checkbox-label"><input type="checkbox" defaultChecked /><span>Remember me</span></label><button type="button" className="text-link">Forgot password?</button></div>{error && <p className="form-error">{error}</p>}<button className="primary-button" type="submit">Sign in <Heart size={17} fill="currentColor" /></button><button className="demo-button" type="button" onClick={() => { setEmail("demo@couplehearts.com"); setPassword("123456"); setError(""); }}><span>🎯</span> Try demo account</button><div className="divider"><span>or continue with</span></div><div className="social-row"><button type="button" className="social-button"><span className="google-g">G</span> Google</button><button type="button" className="social-button"><span className="apple-mark">●</span> Apple</button></div><p className="auth-switch">No account yet? <Link href="/register">Join Couple Hearts <span>💑</span></Link></p></form></AuthShell>;
}
function Register() {
  const [, navigate] = useLocation(); const [form, setForm] = useState({ name: "", email: "", age: "", gender: "", password: "", confirm: "" }); const [agreed, setAgreed] = useState(false); const [error, setError] = useState(""); const update = (key: string, value: string) => setForm((current) => ({ ...current, [key]: value }));
  const submit = (event: React.FormEvent) => { event.preventDefault(); if (!form.name || !form.email || !form.age || !form.gender || form.password.length < 6 || form.password !== form.confirm || !agreed) { setError("Complete the fields, match your passwords, and agree to the terms."); return; } localStorage.setItem(userKey, JSON.stringify({ email: form.email, name: form.name, coins: 1200 })); navigate("/dashboard"); };
  return <AuthShell eyebrow="Start something real" title="Create your account 🚀" subtitle="A few details, then the fun part: meeting someone lovely." sideTitle="Meet with intention."><form className="auth-form register-form" onSubmit={submit}><div className="register-grid"><label>Full name<input value={form.name} onChange={(e) => update("name", e.target.value)} placeholder="Alex Morgan" /></label><label>Email<input value={form.email} onChange={(e) => update("email", e.target.value)} type="email" placeholder="you@example.com" /></label><label>Age<select value={form.age} onChange={(e) => update("age", e.target.value)}><option value="">Select age</option>{Array.from({ length: 43 }, (_, i) => <option key={i + 18}>{i + 18}</option>)}</select></label><label>Gender<select value={form.gender} onChange={(e) => update("gender", e.target.value)}><option value="">Choose one</option><option>Female</option><option>Male</option><option>Other</option></select></label></div><label>Password<input value={form.password} onChange={(e) => update("password", e.target.value)} type="password" placeholder="6+ characters" /></label><label>Confirm password<input value={form.confirm} onChange={(e) => update("confirm", e.target.value)} type="password" placeholder="Repeat your password" /></label><button type="button" className="upload-zone"><span className="upload-icon"><ImagePlus size={20} /></span><span><strong>Add a profile photo</strong><small>Add 2+ photos = 3x more matches 📸</small></span><ChevronRight size={17} /></button><label className="checkbox-label terms"><input type="checkbox" checked={agreed} onChange={(e) => setAgreed(e.target.checked)} /><span>I agree to the Couple Hearts Terms 💑</span></label>{error && <p className="form-error">{error}</p>}<button className="primary-button" type="submit">Create Couple Account <ArrowRight size={17} /></button><p className="auth-switch">Already have an account? <Link href="/login">Sign in</Link></p></form></AuthShell>;
}

const navItems = [{ href: "/dashboard", label: "Dashboard", icon: Sparkles }, { href: "/matches", label: "Matches", icon: Heart, badge: "12" }, { href: "/chats", label: "Chats", icon: MessageCircle, badge: "3" }, { href: "/wallet", label: "Wallet", icon: WalletCards }];
function useCoins() { const [coins, setCoins] = useState(readCoins); useEffect(() => { const sync = () => setCoins(readCoins()); window.addEventListener("couplehearts:coins", sync); window.addEventListener("storage", sync); return () => { window.removeEventListener("couplehearts:coins", sync); window.removeEventListener("storage", sync); }; }, []); return coins; }
function Layout({ children }: { children: ReactNode }) {
  const [location, navigate] = useLocation(); const [mobileOpen, setMobileOpen] = useState(false); const coins = useCoins(); const user = getUser() || defaultUser;
  return <div className="app-shell"><aside className={`sidebar ${mobileOpen ? "sidebar-open" : ""}`}><div><div className="sidebar-top"><Logo compact /><button className="mobile-close" onClick={() => setMobileOpen(false)}><X size={18} /></button></div><p className="sidebar-kicker">YOUR LOVE STORY, ON YOUR TERMS</p><nav className="sidebar-nav">{navItems.map((item) => { const Icon = item.icon; return <Link key={item.href} href={item.href} onClick={() => setMobileOpen(false)} className={location === item.href ? "nav-item active" : "nav-item"}><Icon size={18} /><span>{item.label}</span>{item.badge && <span className="nav-badge">{item.badge}</span>}{item.href === "/wallet" && <span className="coin-count">{coins.toLocaleString()}<CircleDollarSign size={13} /></span>}</Link>; })}</nav></div><div className="sidebar-bottom"><div className="sidebar-tip"><Sparkles size={15} /><span><strong>Profile boost</strong><small>Get seen by more lovely people.</small></span><ChevronRight size={14} /></div><div className="user-card"><div className="avatar avatar-small"><img src={profileImages[5]} /><span className="online-dot" /></div><div className="user-meta"><strong>{user.name}</strong><small>Online now</small></div><button className="icon-button"><Settings size={17} /></button></div><button className="sign-out" onClick={() => { localStorage.removeItem(userKey); navigate("/login"); }}><ArrowRight size={16} /> Sign out</button></div></aside>{mobileOpen && <button className="mobile-scrim" onClick={() => setMobileOpen(false)} aria-label="Close navigation" />}<main className="main-content"><header className="mobile-header"><button className="icon-button" onClick={() => setMobileOpen(true)}><Menu size={21} /></button><Logo compact /><button className="icon-button"><Bell size={19} /></button></header>{children}</main><nav className="mobile-nav">{navItems.map((item) => { const Icon = item.icon; return <Link key={item.href} href={item.href} className={location === item.href ? "mobile-nav-item active" : "mobile-nav-item"}><Icon size={19} /><span>{item.label}</span></Link>; })}</nav></div>;
}
function PageHeader({ eyebrow, title, description, action }: { eyebrow: string; title: string; description: string; action?: ReactNode }) { return <div className="page-header"><div><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p className="page-description">{description}</p></div>{action}</div>; }
function LowCoinsModal({ needed, onClose }: { needed: number; onClose: () => void }) { const coins = useCoins(); return <div className="modal-backdrop" onClick={onClose}><motion.div className="utility-modal" initial={{ opacity: 0, scale: .95, y: 10 }} animate={{ opacity: 1, scale: 1, y: 0 }} onClick={(event) => event.stopPropagation()}><button className="modal-close" onClick={onClose}><X size={18} /></button><div className="utility-icon gold"><CircleDollarSign size={25} /></div><p className="eyebrow">Your sparkle needs a top-up</p><h2>Need more coins?</h2><p>You need {needed} coins, but your balance is {coins}. Add a little magic to keep connecting.</p><Link href="/wallet" className="primary-button" onClick={onClose}>Top up wallet <WalletCards size={17} /></Link><button className="text-link keep-swiping" onClick={onClose}>Maybe later</button></motion.div></div>; }

function FilterModal({ value, onChange, onClose, onApply }: { value: { minAge: number; maxAge: number; distance: number; interests: string[]; verified: boolean }; onChange: (next: typeof value) => void; onClose: () => void; onApply: () => void }) {
  const allInterests = Array.from(new Set(profiles.flatMap((profile) => profile.interests)));
  const toggle = (interest: string) => onChange({ ...value, interests: value.interests.includes(interest) ? value.interests.filter((item) => item !== interest) : [...value.interests, interest] });
  return <div className="modal-backdrop" onClick={onClose}><motion.div className="utility-modal filter-modal" initial={{ opacity: 0, scale: .95, y: 10 }} animate={{ opacity: 1, scale: 1, y: 0 }} onClick={(event) => event.stopPropagation()}><button className="modal-close" onClick={onClose}><X size={18} /></button><p className="eyebrow">Curate your serendipity</p><h2>Filters</h2><div className="filter-grid"><label>Age range<div className="range-row"><input type="number" min="18" max="70" value={value.minAge} onChange={(e) => onChange({ ...value, minAge: Number(e.target.value) })} /><span>to</span><input type="number" min="18" max="70" value={value.maxAge} onChange={(e) => onChange({ ...value, maxAge: Number(e.target.value) })} /></div></label><label>Distance <select value={value.distance} onChange={(e) => onChange({ ...value, distance: Number(e.target.value) })}><option value="10">Within 10 km</option><option value="20">Within 20 km</option><option value="50">Within 50 km</option></select></label></div><div className="filter-label">Interests</div><div className="interest-picker">{allInterests.map((interest) => <button type="button" key={interest} className={value.interests.includes(interest) ? "interest-choice selected" : "interest-choice"} onClick={() => toggle(interest)}>{interest}{value.interests.includes(interest) && <Check size={13} />}</button>)}</div><label className="checkbox-label filter-check"><input type="checkbox" checked={value.verified} onChange={(e) => onChange({ ...value, verified: e.target.checked })} /><span>Only show verified profiles</span></label><button className="primary-button" onClick={onApply}>Show my matches <ArrowRight size={17} /></button></motion.div></div>;
}

function Dashboard() {
  const [index, setIndex] = useState(0); const [history, setHistory] = useState<{ profile: Profile; action: string }[]>([]); const [matchProfile, setMatchProfile] = useState<Profile | null>(null); const [filterOpen, setFilterOpen] = useState(false); const [boost, setBoost] = useState(0); const [lowCoins, setLowCoins] = useState(0); const [filters, setFilters] = useState({ minAge: 22, maxAge: 35, distance: 25, interests: [] as string[], verified: false });
  const x = useMotionValue(0); const y = useMotionValue(0); const rotate = useTransform(x, [-260, 260], [-16, 16]); const likeOpacity = useTransform(x, [35, 120], [0, 1]); const nopeOpacity = useTransform(x, [-120, -35], [1, 0]); const superOpacity = useTransform(y, [-150, -55], [1, 0]); const controls = useAnimationControls();
  const filtered = useMemo(() => profiles.filter((profile) => profile.age >= filters.minAge && profile.age <= filters.maxAge && profile.distance <= filters.distance && (!filters.verified || profile.verified) && (!filters.interests.length || profile.interests.some((interest) => filters.interests.includes(interest)))), [filters]);
  const current = filtered[index];
  useEffect(() => { setIndex(0); setHistory([]); controls.set({ x: 0, y: 0, rotate: 0 }); }, [filters, controls]);
  useEffect(() => { if (!boost) return; const timer = window.setInterval(() => setBoost((value) => value <= 1 ? 0 : value - 1), 1000); return () => window.clearInterval(timer); }, [boost]);
  const doMatch = (profile: Profile, action: string) => { if (action !== "like" && action !== "super") return; const isMatch = action === "super" || profile.id % 3 === 1; if (!isMatch) return; const currentMatches = loadMatches(); if (!currentMatches.some((item) => item.id === profile.id)) saveMatches([{ ...profile, matchedAt: Date.now(), isNew: true }, ...currentMatches]); setMatchProfile(profile); };
  const finishSwipe = (action: "pass" | "like" | "super") => { if (!current) return; setHistory((items) => [...items, { profile: current, action }]); setIndex((value) => value + 1); x.set(0); y.set(0); controls.set({ x: 0, y: 0, rotate: 0, opacity: 1 }); doMatch(current, action); vibrate(action === "pass" ? 10 : [15, 40, 15]); };
  const swipe = async (action: "pass" | "like" | "super") => { if (!current) return; if (action === "super" && !spendCoins(20)) { setLowCoins(20); return; } const exit = action === "pass" ? -500 : 500; await controls.start({ x: exit, y: action === "super" ? -240 : 0, rotate: action === "pass" ? -18 : 18, opacity: 0, transition: { duration: .24 } }); finishSwipe(action); };
  const rewind = async () => { if (!history.length) return; if (!spendCoins(50)) { setLowCoins(50); return; } setIndex((value) => Math.max(0, value - 1)); setHistory((items) => items.slice(0, -1)); vibrate(20); };
  const startBoost = () => { if (!spendCoins(100)) { setLowCoins(100); return; } setBoost(1800); vibrate([20, 50, 20]); };
  const dragEnd = (_: unknown, info: { offset: { x: number; y: number } }) => { if (info.offset.y < -100) void swipe("super"); else if (info.offset.x > 100) void swipe("like"); else if (info.offset.x < -100) void swipe("pass"); else void controls.start({ x: 0, y: 0, rotate: 0 }); };
  return <div className="page dashboard-page"><PageHeader eyebrow="Tuesday, September 18" title="Discover couples 💑" description="A little serendipity, curated just for you." action={<div className="header-actions"><button className="ghost-button" onClick={() => setFilterOpen(true)}><SlidersHorizontal size={16} /> Filters {filters.interests.length > 0 && <Pill tone="rose">{filters.interests.length}</Pill>}</button><button className="premium-button"><Crown size={16} fill="currentColor" /> Go premium · $6.99</button></div>} /><div className="discover-layout"><section className="discover-stage"><div className="stage-label"><span><span className="live-dot" /> Curated for Alex</span><span>{current ? `${index + 1} of ${filtered.length}` : "Deck complete"}</span></div><div className="card-stack">{current ? <><div className="back-card back-card-two stack-preview"><img src={filtered[index + 2]?.images[0] || current.images[0]} alt="Next profile" /></div><div className="back-card back-card-one stack-preview"><img src={filtered[index + 1]?.images[0] || current.images[0]} alt="Next profile" /></div><motion.article className="swipe-card" style={{ x, y, rotate }} drag dragConstraints={{ left: 0, right: 0, top: 0, bottom: 0 }} onDragEnd={dragEnd}><img src={current.images[0]} alt={current.name} /><div className="card-overlay" /><motion.div className="swipe-label like-label" style={{ opacity: likeOpacity }}>LIKE</motion.div><motion.div className="swipe-label nope-label" style={{ opacity: nopeOpacity }}>NOPE</motion.div><motion.div className="swipe-label super-label" style={{ opacity: superOpacity }}>SUPER LIKE</motion.div><div className="card-content"><div className="profile-topline"><span className="verified-badge">{current.verified && <><ShieldCheck size={13} /> Verified</>}</span><span className="profile-match">{current.match}% match</span></div><div><h2>{current.name}, <span>{current.age}</span></h2><p className="profile-location"><MapPin size={14} /> {current.city} <span>·</span> {current.distance} km away {current.online && <><span>·</span><span className="card-online">online now</span></>}</p><p className="profile-bio">{current.bio}</p><div className="profile-tags">{current.interests.map((tag) => <span key={tag}>{tag}</span>)}</div></div></div></motion.article></> : <div className="deck-empty"><div className="utility-icon"><Heart size={24} /></div><h2>You saw everyone nearby.</h2><p>Try widening your filters or rewind your last decision.</p><div className="deck-empty-actions"><button className="ghost-button" onClick={() => setFilterOpen(true)}>Change filters</button><button className="primary-button" onClick={rewind} disabled={!history.length}>Rewind 50 <CircleDollarSign size={15} /></button></div></div>}</div>{current && <><div className="swipe-actions"><button className="swipe-action pass" onClick={() => void swipe("pass")} aria-label="Pass"><X size={25} /></button><button className="swipe-action rewind" onClick={() => void rewind()} aria-label="Rewind"><ArrowLeft size={20} /></button><button className="swipe-action super" onClick={() => void swipe("super")} aria-label="Super like"><Star size={22} fill="currentColor" /></button><button className="swipe-action like" onClick={() => void swipe("like")} aria-label="Like"><Heart size={30} fill="currentColor" /></button><button className="swipe-action boost" onClick={startBoost} aria-label="Boost"><Zap size={20} fill="currentColor" /></button></div><p className="swipe-hint"><span>←</span> Swipe to explore <span>↑ super like</span> <span>→</span></p></>}</section><aside className="discover-aside"><div className="aside-card mini-profile"><div className="mini-profile-head"><div><p className="eyebrow">Your profile</p><h3>Looking lovely, Alex.</h3></div><button className="icon-button"><MoreHorizontal size={18} /></button></div><div className="profile-completion"><div className="completion-ring"><span>74%</span></div><div><strong>Almost there</strong><p>Add one more photo to stand out.</p><button className="text-link">Complete profile <ArrowRight size={13} /></button></div></div></div><div className="aside-card intention-card"><div className="intention-icon"><HeartHandshake size={18} /></div><div><p className="eyebrow">Today’s intention</p><h3>Be open to a plot twist.</h3><p>Great connections rarely look exactly like the plan.</p></div></div><div className="aside-card match-teaser"><div className="teaser-top"><p className="eyebrow">Your week in love</p><span className="trend"><ArrowRight size={13} /> 18%</span></div><div className="teaser-stat"><strong>12</strong><span>new possibilities<br />this week</span></div><div className="tiny-avatars">{profileImages.slice(0, 5).map((src, i) => <img key={src} src={src} style={{ zIndex: 5 - i }} alt="" />)}<span>+7</span></div></div></aside></div>{boost > 0 && <div className="boost-pill"><Zap size={14} fill="currentColor" /> Boost active · {String(Math.floor(boost / 60)).padStart(2, "0")}:{String(boost % 60).padStart(2, "0")}</div>}{filterOpen && <FilterModal value={filters} onChange={setFilters} onClose={() => setFilterOpen(false)} onApply={() => setFilterOpen(false)} />}{matchProfile && <MatchModal profile={matchProfile} onClose={() => setMatchProfile(null)} />}{lowCoins > 0 && <LowCoinsModal needed={lowCoins} onClose={() => setLowCoins(0)} />}</div>;
}
function MatchModal({ profile, onClose }: { profile: Profile; onClose: () => void }) { return <div className="modal-backdrop" onClick={onClose}><motion.div className="match-modal" initial={{ opacity: 0, scale: .94, y: 12 }} animate={{ opacity: 1, scale: 1, y: 0 }} onClick={(event) => event.stopPropagation()}><div className="confetti">{Array.from({ length: 18 }, (_, i) => <span key={i} style={{ ["--i" as string]: i } as React.CSSProperties} />)}</div><button className="modal-close" onClick={onClose}><X size={18} /></button><div className="match-hearts"><Heart size={28} fill="currentColor" /><Heart size={42} fill="currentColor" /><Heart size={28} fill="currentColor" /></div><p className="eyebrow">It’s a match</p><h2>You & {profile.name} are a couple! 💑❤️</h2><p>That little spark is mutual. Start with something easy and make it yours.</p><div className="match-people"><div><img src={profileImages[5]} alt="Alex" /><span>Alex</span></div><Heart size={18} fill="currentColor" /><div><img src={profile.images[0]} alt={profile.name} /><span>{profile.name}</span></div></div><Link href={`/chats?user=${profile.id}`} className="primary-button" onClick={onClose}>Chat now <MessageCircle size={17} /></Link><button className="text-link keep-swiping" onClick={onClose}>Keep swiping</button></motion.div></div>; }

function Matches() {
  const [tab, setTab] = useState<"All" | "Liked You" | "New Matches">("All"); const [items, setItems] = useState<MatchRecord[]>(loadMatches); const [hovered, setHovered] = useState<number | null>(null); const [remove, setRemove] = useState<MatchRecord | null>(null);
  useEffect(() => { const sync = () => setItems(loadMatches()); window.addEventListener("couplehearts:matches", sync); return () => window.removeEventListener("couplehearts:matches", sync); }, []);
  const shown = tab === "New Matches" ? items.filter((item) => item.isNew) : items;
  const confirmRemove = () => { if (!remove) return; const next = items.filter((item) => item.id !== remove.id); setItems(next); saveMatches(next); setRemove(null); };
  return <div className="page"><PageHeader eyebrow="The good stuff" title="Your matches ❤️" description="People who make your heart do that little skip." action={<Link href="/dashboard" className="ghost-button"><Search size={17} /> Discover more</Link>} /><div className="tabs-row">{(["All", "Liked You", "New Matches"] as const).map((item) => <button key={item} className={tab === item ? "tab active" : "tab"} onClick={() => setTab(item)}>{item}{item === "Liked You" ? <Lock size={11} /> : item === "New Matches" && <span>{items.filter((match) => match.isNew).length}</span>}</button>)}</div>{tab === "Liked You" ? <div className="liked-you-panel"><div className="lock-orbit"><Lock size={22} /></div><p className="eyebrow">Premium admirers</p><h2>Someone already likes you.</h2><p>Unlock your {Math.max(4, items.length + 2)} admirers and let the good energy find you faster.</p><button className="premium-button"><Crown size={15} fill="currentColor" /> Unlock with premium</button><div className="blurred-admirers">{items.slice(0, 3).map((item) => <img key={item.id} src={item.images[0]} alt="" />)}</div></div> : <div className="matches-grid">{shown.map((match, i) => <motion.article key={match.id} className="match-card" initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: i * .05 }} onMouseEnter={() => setHovered(match.id)} onMouseLeave={() => setHovered(null)}><div className="match-image-wrap"><img src={hovered === match.id ? match.images[1] : match.images[0]} alt={match.name} /><div className="match-image-gradient" /><span className="percent-badge">{matchPercent(match)}% match</span><button className="match-card-menu" onClick={() => setRemove(match)} aria-label={`Unmatch ${match.name}`}><MoreHorizontal size={17} /></button><div className="match-card-info"><h3>{match.name}, {match.age}</h3><p>{match.role} · {match.city}</p></div></div><div className="match-card-footer"><span className={match.isNew ? "status-new" : "status-muted"}>{match.online && <span className="live-dot" />}{match.isNew ? "New match" : match.online ? "Active now" : `Active ${timeLabel(match.matchedAt)}`}</span><Link href={`/chats?user=${match.id}`} className="round-message" aria-label={`Message ${match.name}`}><MessageCircle size={16} /></Link></div></motion.article>)}{!shown.length && <div className="empty-state"><Heart size={22} /><h3>No new matches yet</h3><p>Keep swiping. Your next lovely conversation is out there.</p><Link href="/dashboard" className="primary-button">Discover people <ArrowRight size={16} /></Link></div>}<article className="premium-lock-card"><div className="lock-orbit"><Lock size={22} /></div><p className="eyebrow">A little mystery</p><h3>See who already likes you.</h3><p>Unlock your admirers and let the good energy find you faster.</p><button className="premium-button"><Crown size={15} fill="currentColor" /> Unlock with premium</button></article></div>}{remove && <div className="modal-backdrop" onClick={() => setRemove(null)}><motion.div className="utility-modal" initial={{ opacity: 0, scale: .95 }} animate={{ opacity: 1, scale: 1 }} onClick={(event) => event.stopPropagation()}><div className="utility-icon rose"><Trash2 size={22} /></div><p className="eyebrow">Make space for new sparks</p><h2>Unmatch {remove.name}?</h2><p>Your conversation and match will be removed from Couple Hearts. You can always meet again.</p><div className="modal-actions"><button className="ghost-button" onClick={() => setRemove(null)}>Keep match</button><button className="danger-button" onClick={confirmRemove}>Unmatch</button></div></motion.div></div>}</div>;
}

function initialChats(): ChatRecord[] { return loadMatches().slice(0, 4).map((profile, index) => ({ userId: profile.id, lastAt: Date.now() - index * 3600000, messages: [{ id: `seed-${profile.id}-1`, sender: "them", text: "Hey Alex! Your profile has such a good energy.", at: Date.now() - 7200000, read: true }, { id: `seed-${profile.id}-2`, sender: "me", text: "That’s the loveliest thing to hear. Yours made me smile too ✨", at: Date.now() - 7100000, read: true }, { id: `seed-${profile.id}-3`, sender: "them", text: profile.id === 1 ? "A walk through El Born sounds perfect ✨" : "What’s something you’re excited about this week?", at: Date.now() - 7000000, read: true }] })); }
function loadChats() { const stored = readJson<ChatRecord[]>(chatKey, []); return stored.length ? stored : initialChats(); }
function Chats() {
  const [, navigate] = useLocation(); const [records, setRecords] = useState<ChatRecord[]>(loadChats); const [selectedId, setSelectedId] = useState<number>(() => Number(new URLSearchParams(window.location.search).get("user")) || loadChats()[0]?.userId || 1); const [query, setQuery] = useState(""); const [message, setMessage] = useState(""); const [emojiOpen, setEmojiOpen] = useState(false); const [giftOpen, setGiftOpen] = useState(false); const [imageOpen, setImageOpen] = useState(false); const [typing, setTyping] = useState(false); const [lowCoins, setLowCoins] = useState(0); const bodyRef = useRef<HTMLDivElement>(null);
  const activeRecord = records.find((record) => record.userId === selectedId) || records[0]; const active = profileById(activeRecord?.userId || 1); const sorted = [...records].sort((a, b) => b.lastAt - a.lastAt).filter((record) => profileById(record.userId).name.toLowerCase().includes(query.toLowerCase()));
  useEffect(() => { localStorage.setItem(chatKey, JSON.stringify(records)); }, [records]);
  useEffect(() => { bodyRef.current?.scrollTo({ top: bodyRef.current.scrollHeight, behavior: "smooth" }); }, [activeRecord?.messages.length, typing]);
  useEffect(() => { const id = window.setTimeout(() => setTyping(true), 2000); return () => window.clearTimeout(id); }, [selectedId]);
  const selectChat = (id: number) => { setSelectedId(id); navigate(`/chats?user=${id}`); setTyping(false); setGiftOpen(false); setImageOpen(false); };
  const append = (newMessage: ChatMessage, autoReply = false) => { setRecords((current) => current.map((record) => record.userId === selectedId ? { ...record, lastAt: newMessage.at, messages: [...record.messages, newMessage] } : record)); if (autoReply) { setTyping(false); window.setTimeout(() => { const reply: ChatMessage = { id: `reply-${Date.now()}`, sender: "them", text: "I love that. Tell me more? ✨", at: Date.now(), read: true }; setRecords((current) => current.map((record) => record.userId === selectedId ? { ...record, lastAt: reply.at, messages: [...record.messages, reply] } : record)); }, 1100); } };
  const sendText = (event: React.FormEvent) => { event.preventDefault(); if (!message.trim() || !activeRecord) return; append({ id: `message-${Date.now()}`, sender: "me", text: message.trim(), at: Date.now(), read: true }, true); setMessage(""); setEmojiOpen(false); vibrate(10); };
  const sendImage = (image: string) => { append({ id: `image-${Date.now()}`, sender: "me", image, at: Date.now(), read: true }); setImageOpen(false); };
  const sendGift = (gift: string, cost: number) => { if (!spendCoins(cost)) { setLowCoins(cost); return; } append({ id: `gift-${Date.now()}`, sender: "me", gift, text: `Sent a ${gift}`, at: Date.now(), read: true }); setGiftOpen(false); vibrate([12, 35, 12]); };
  return <div className="page chats-page"><PageHeader eyebrow="Good conversations" title="Your chats 🥰" description="Keep the spark going, one message at a time." action={<Link href="/matches" className="ghost-button"><UsersRound size={17} /> New connection</Link>} /><div className="chat-layout"><aside className="chat-list"><div className="chat-search"><Search size={16} /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search chats" /></div><div className="chat-list-head"><span>Messages</span><span>{records.filter((record) => record.messages.some((item) => item.sender === "them" && !item.read)).length} unread</span></div>{sorted.map((record) => { const person = profileById(record.userId); const last = record.messages[record.messages.length - 1]; return <button key={record.userId} className={selectedId === record.userId ? "chat-preview selected" : "chat-preview"} onClick={() => selectChat(record.userId)}><div className="avatar"><img src={person.images[0]} alt={person.name} /><span className={person.online ? "online-dot" : "online-dot offline"} /></div><div className="chat-preview-copy"><div><strong>{person.name}</strong><span>{timeLabel(record.lastAt)}</span></div><p>{last?.image ? "Photo" : last?.gift ? `Gift: ${last.gift}` : last?.text}</p></div>{record.messages.filter((item) => item.sender === "them" && !item.read).length > 0 && <span className="unread-badge">{record.messages.filter((item) => item.sender === "them" && !item.read).length}</span>}</button>; })}<div className="chat-list-bottom"><Sparkles size={16} /><span>Tip: ask about something in their profile.</span></div></aside><section className="conversation"><header className="conversation-head"><div className="conversation-person"><div className="avatar"><img src={active.images[0]} alt={active.name} /><span className={active.online ? "online-dot" : "online-dot offline"} /></div><div><h3>{active.name}</h3><p>{active.online ? "Online now" : "Last seen today"}</p></div></div><div className="conversation-actions"><button className="icon-button" aria-label="Video call"><Video size={18} /></button><button className="icon-button" aria-label="More actions"><MoreHorizontal size={18} /></button></div></header><div className="conversation-body" ref={bodyRef}><div className="day-divider"><span>Today</span></div><AnimatePresence initial={false}>{activeRecord?.messages.map((item) => <motion.div key={item.id} className={`message-row ${item.sender === "me" ? "mine" : "theirs"}`} initial={{ opacity: 0, y: 8, scale: .98 }} animate={{ opacity: 1, y: 0, scale: 1 }}><>{item.sender === "them" && <img src={active.images[0]} alt={active.name} />}</><div>{item.image ? <img className="sent-image" src={item.image} alt="Shared" /> : item.gift ? <div className="gift-bubble"><Gift size={18} /><strong>{item.gift}</strong><span>{item.text}</span></div> : <div className="bubble"><p>{item.text}</p></div>}<small>{timeLabel(item.at)} {item.sender === "me" && <CheckCheck className="read-receipt" size={13} />}</small></div></motion.div>)}</AnimatePresence>{typing && <div className="typing-row"><img src={active.images[0]} alt={active.name} /><div className="typing-bubble"><i /><i /><i /></div></div>}</div><div className="composer-wrap"><div className="composer-tools"><div className="tool-popover-wrap"><button type="button" className="composer-tool" onClick={() => { setGiftOpen(!giftOpen); setImageOpen(false); }}><Gift size={15} /> Gifts</button>{giftOpen && <div className="tool-popover gift-picker">{[["Rose", 10], ["Teddy", 50], ["Diamond", 100]].map(([gift, cost]) => <button key={gift as string} onClick={() => sendGift(gift as string, cost as number)}><span>{gift === "Rose" ? "🌹" : gift === "Teddy" ? "🧸" : "💎"}</span><b>{gift}</b><small>{cost} coins</small></button>)}</div>}</div><div className="tool-popover-wrap"><button type="button" className="composer-tool" onClick={() => { setImageOpen(!imageOpen); setGiftOpen(false); }}><ImagePlus size={15} /> Photo</button>{imageOpen && <div className="tool-popover image-picker"><span>Share a little moment</span><div>{active.images.map((image) => <button key={image} onClick={() => sendImage(image)}><img src={image} alt="Choose" /></button>)}</div></div>}</div><div className="tool-popover-wrap"><button type="button" className="composer-tool" onClick={() => setEmojiOpen(!emojiOpen)}>✨ Emoji</button>{emojiOpen && <div className="tool-popover emoji-picker">{["✨", "🥰", "☕", "🌙", "😂", "❤️", "🌹", "🙌"].map((emoji) => <button key={emoji} onClick={() => { setMessage((value) => value + emoji); setEmojiOpen(false); }}>{emoji}</button>)}</div>}</div></div><form className="composer" onSubmit={sendText}><input value={message} onChange={(e) => setMessage(e.target.value)} placeholder={`Write ${active.name} a thoughtful message...`} /><button className="send-button" type="submit"><Send size={17} /></button></form></div></section></div>{lowCoins > 0 && <LowCoinsModal needed={lowCoins} onClose={() => setLowCoins(0)} />}</div>;
}

function Wallet() {
  const coins = useCoins(); const [notice, setNotice] = useState(""); const packages = [{ coins: 100, price: "$2", label: "A little boost" }, { coins: 500, price: "$8", label: "Most loved", popular: true }, { coins: 1200, price: "$15", label: "Best value" }]; const buy = (pack: typeof packages[number]) => { updateCoins(pack.coins); setNotice(`${pack.coins.toLocaleString()} coins added to your wallet.`); window.setTimeout(() => setNotice(""), 2600); };
  return <div className="page wallet-page"><PageHeader eyebrow="A little extra magic" title="Your wallet 💰" description="More ways to make your intentions known." action={<div className="balance-chip"><CircleDollarSign size={16} /> {coins.toLocaleString()} coins</div>} /><div className="wallet-grid"><section className="balance-card"><div className="balance-glow" /><div className="balance-card-top"><span className="wallet-icon"><CircleDollarSign size={21} /></span><span className="balance-label">Current balance</span><button className="icon-button light"><MoreHorizontal size={18} /></button></div><div className="balance-number">{coins.toLocaleString()}</div><div className="balance-caption">COUPLE COINS <span>·</span> Ready to spend on connection</div><div className="balance-card-foot"><span><span className="live-dot" /> Your wallet is active</span><button className="light-text-button">Transaction history <ArrowRight size={14} /></button></div></section><section className="wallet-section"><div className="section-heading"><div><p className="eyebrow">Choose your energy</p><h2>Top up coins</h2></div><span className="secure-label"><Lock size={13} /> Secure checkout</span></div><div className="coin-packages">{packages.map((pack) => <button key={pack.coins} className={pack.popular ? "coin-package popular" : "coin-package"} onClick={() => buy(pack)}>{pack.popular && <span className="popular-label">Popular</span>}<span className="coin-symbol">◉</span><strong>{pack.coins.toLocaleString()}</strong><span>coins</span><b>{pack.price}</b><small>{pack.label}</small></button>)}</div></section><section className="wallet-section perks-section"><div className="section-heading"><div><p className="eyebrow">Spend them on</p><h2>Little gestures, big energy</h2></div></div><div className="perk-grid"><Perk icon={<Zap size={19} />} title="Boost" value="100 coins" description="Be seen by more compatible people." /><Perk icon={<Star size={19} />} title="Super like" value="20 coins" description="Let someone know they really caught your eye." /><Perk icon={<Crown size={19} />} title="Premium" value="$6.99 / month" description="Unlimited likes, rewind, and no ads." premium /></div></section><section className="payment-card"><div className="payment-icon"><CreditCard size={20} /></div><div><p className="eyebrow">Simple & secure</p><h3>Pay the way that feels easy.</h3><p>Cards, Paystack, and M-Pesa are all welcome here.</p></div><button className="icon-button"><ChevronRight size={18} /></button></section></div>{notice && <motion.div className="toast" initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }}><Check size={16} /> {notice}</motion.div>}</div>;
}
function Perk({ icon, title, value, description, premium = false }: { icon: ReactNode; title: string; value: string; description: string; premium?: boolean }) { return <article className="perk-card"><div className={premium ? "perk-icon premium-icon" : "perk-icon"}>{icon}</div><div><div className="perk-name"><h3>{title}</h3>{premium && <Crown size={14} fill="currentColor" />}</div><strong>{value}</strong><p>{description}</p></div><button className="round-arrow"><ArrowRight size={15} /></button></article>; }
function Protected({ children }: { children: ReactNode }) { const [, navigate] = useLocation(); const user = getUser(); useEffect(() => { if (!user) navigate("/login"); }, [navigate, user]); return user ? <Layout>{children}</Layout> : null; }
function RootRedirect() { const [, navigate] = useLocation(); useEffect(() => { navigate(getUser() ? "/dashboard" : "/login"); }, [navigate]); return null; }
export default function App() { return <Switch><Route path="/" component={RootRedirect} /><Route path="/login" component={Login} /><Route path="/register" component={Register} /><Route path="/dashboard"><Protected><Dashboard /></Protected></Route><Route path="/matches"><Protected><Matches /></Protected></Route><Route path="/chats"><Protected><Chats /></Protected></Route><Route path="/wallet"><Protected><Wallet /></Protected></Route><Route><RootRedirect /></Route></Switch>; }
